import {Section} from '@components/layout/section'
import 'server-only'

export default async function Contact() {
  return (
    <main>
      <Section className="pt-24">Contact</Section>
    </main>
  )
}
