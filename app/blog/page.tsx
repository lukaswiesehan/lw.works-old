import {Section} from '@components/layout/section'
import 'server-only'

export default async function Blog() {
  return (
    <main>
      <Section className="pt-24">Blog</Section>
    </main>
  )
}
