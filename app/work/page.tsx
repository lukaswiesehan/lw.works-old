import {Section} from '@components/layout/section'
import 'server-only'

export default async function Work() {
  return (
    <main>
      <Section className="pt-24">Work</Section>
    </main>
  )
}
